import { FashionFilterPipe } from './fashion-filter-pipe';

describe('FashionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FashionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
